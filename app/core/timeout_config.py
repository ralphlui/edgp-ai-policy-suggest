# Optimized timeout settings for faster response
LLM_TIMEOUT_SECONDS = 12    # Reduced LLM timeout for faster response
VECTOR_DB_TIMEOUT = 3       # Reduced vector DB timeout 
AGENT_STEP_TIMEOUT = 3      # Reduced agent step timeout
TOTAL_REQUEST_TIMEOUT = 25  # Target total request time
LLM_MAX_TOKENS = 1024      # Reduced max tokens for faster responses